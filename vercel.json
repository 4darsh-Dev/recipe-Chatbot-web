{
    "builds": [{
        "src": "repieChatbot/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "repieChatbot/wsgi.py"
        }
    ]
}